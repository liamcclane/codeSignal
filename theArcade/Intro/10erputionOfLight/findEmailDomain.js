/***Find Email Domain
 * @param {string}
 * @return {string}
 */
function findEmailDomain(address) {

    /**Valid Email
     * if blah blah
     * @param {string} email
     * @return {boolean}
     */
    const validEmail = (email) => {
        return true;
    }

    if(!validEmail(address)) {return ""}
}
